apiVersion: v1
kind: Service
metadata:
  name: datacollectionservice
  namespace: getrand
  labels:
    service: datacollectionservice
spec:
  selector:
    service: datacollectionservice
  ports:
    - port: 5003
      targetPort: 5003
  type: ClusterIP
